{
  "scripts": {
    "start": "live-server . --port=3000 --host=localhost --no-browser",
    "format": "prettier . --check",
    "avif": "--input='./src/assets/apple/*.{jpg,jpeg}' --output='./src/assets/apple' --overwrite"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "prettier": "^3.5.3"
  }
}
